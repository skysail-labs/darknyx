{
  "name": "@darknyx/circuits",
  "version": "0.1.0",
  "private": true,
  "description": "Circom ZK circuits for the Darknyx dark pool (VALID_SPEND, VALID_WALLET_CREATE, etc.)",
  "scripts": {
    "build": "bash ../scripts/build-circuits.sh",
    "test": "vitest run"
  },
  "dependencies": {
    "circom_tester": "0.0.24",
    "circomlib": "^2.0.5"
  },
  "devDependencies": {
    "snarkjs": "^0.7.5",
    "vitest": "^4.1.7"
  }
}
