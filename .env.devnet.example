# Copy to .env.devnet and fill in. Loaded by
# packages/sdk/tests/devnet-setup.test.ts (the RUN_DEVNET_E2E setup flow);
# every value below also has a sensible in-test default, so .env.devnet is
# optional. Most devnet/CVM flows pass env inline — see scripts/dev-commands.md.
L1_RPC_URL=https://api.devnet.solana.com
VAULT_PROGRAM_ID=C63vKvysCzX55PKraas4Wc22ijqjGJQdPC1mrzCFVWZx
PROTOCOL_FEE_BPS=30
DEMO_MINT_DECIMALS=6
